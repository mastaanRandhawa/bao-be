node_modules
dist
dist-ssr
*.local
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Playwright audit artifacts
.playwright-mcp
home-full.png
home-viewport.png
home-mobile.png

.agents
.claude

# Keep public assets (logo, hero) tracked — do not ignore public/
# docs/, assets/, images/, index.html, 404.html at repo root are committed
# production builds for GitHub Pages (updated by CI postbuild sync)
